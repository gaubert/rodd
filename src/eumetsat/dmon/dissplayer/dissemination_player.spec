# -*- mode: python -*-
a = Analysis(['dissemination_player.py'],
             pathex=['/home/gmv/Dev/rodd/src/eumetsat/dmon/dissplayer'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='diss_player',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='dissemination_player')
